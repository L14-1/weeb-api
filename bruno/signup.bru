meta {
  name: signup
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/signup/
  body: json
  auth: none
}

body:json {
  {
    "first_name": "{{firstName}}",
    "last_name": "{{lastName}}",
    "email": "{{email}}",
    "password": "{{password}}"
  }
}
